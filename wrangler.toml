name = "imresidentdashboardapp"
pages_build_output_dir = "frontend"
compatibility_date = "2026-07-01"

[[d1_databases]]
binding = "DB"
database_name = "attendance-db"
database_id = "b989ffda-3604-4f37-a13d-e1c685848f9c"

[vars]
# nicholasbrazeau.com is verified in Resend (DKIM/SPF/DMARC), so sign-in
# emails send from a real address instead of the onboarding@resend.dev
# sandbox — this is required for delivery to actual resident @duke.edu
# inboxes, not just the account owner's own verified address.
RESEND_FROM = "noreply@nicholasbrazeau.com"
