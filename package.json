{
  "name": "im-resident-attendance-app",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev frontend",
    "deploy": "wrangler pages deploy frontend",
    "test": "node --test"
  },
  "devDependencies": {
    "wrangler": "^4.110.0"
  }
}
